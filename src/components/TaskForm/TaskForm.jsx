// Import modules
import React from 'react';

// Create the function for the add task form
function TaskForm() {
    return (
        <form>
        
        </form>
    );
};

// Export function to app
export default TaskForm;